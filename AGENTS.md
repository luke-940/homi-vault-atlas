# Homi Vault Atlas v7.5 working contract

Before any task, read `docs/VISUALIZATION-NORTH-STAR.md` and the current Gate Understanding Packet supplied with the task evidence.

The product is a commercial-grade knowledge visualization, not an MVP, demo, prototype, or temporary experiment. Every visual channel must encode real data, every public byte must respect the Owner/Public boundary, and every interaction must have an accessible textual equivalent.

The approved visual direction is the true-3D knowledge field recorded in the local visual evidence. Home is a full-viewport spatial field with a translucent editorial veil, not a dashboard card beside a flat map. X is district, Y is freshness, and Z is structural depth. Use a modern flat point-field grammar over a restrained cosmic ink/indigo/violet background; only reconciled record marks and real relation routes may read as stars or paths. Preserve the same selected knowledge ID and edge direction across Home, Explore, Observe, Flow, and Search. Runtime force drift, auto-rotation, decorative particles, and camera motion without a user/data event remain forbidden.

Home must not use filled district panels, pastel territory skins, or explanatory cards over the graph. District identity is encoded by position, hue, and direct labels. Node size, core brightness, and bounded halo reach must increase monotonically with `uniqueInboundDocuments`; that luminance stays local to the node and renders behind real routes. The canvas remains unobstructed, while detailed evidence moves to Explore/Observe and screen readers receive a non-visual live status.

## Owner semantic focus gate

- The local Owner product is the primary v7.5 surface. Every admitted Owner source title stays byte-for-byte equal to its graph label and remains searchable; public aliases and title masking are forbidden in the Owner profile.
- Every visible graph line originates from a `RenderEdgeCommand` whose kind is exactly `district_corridor`, `exact_reference`, or `directed_path`, with provenance `atlas.graph.v1`. Membership, aggregate density, decoration, and layout guides never become lines.
- Home overview shows at most the four strongest district pairs, with at most two factual direction lanes per pair. Node preview shows at most six incoming and six outgoing references and reports hidden counts.
- `previewId` is transient and never serialized. Only click, Enter, or tap commits `focusId` to the URL; pointer leave and blur restore the committed selection.
- A screenshot or hash is never visual proof by itself. Compare the selected concept and implementation at the same viewport, run geometry checks independently, and keep `design-qa.md` current.
- Local QA is serial, worker 1, and bounded. Never revive the retired 24-route by 5-repeat local performance run.

Do not modify Luke Vault, Notion, `graph.json`, Current State, Batch/cursor, Daily, Rocket, Groot, or HIL. Do not publish Owner data or internal evidence. Stop on privacy leakage, count mismatch, torn capture, false directional edges, nondeterministic layout, hairball rendering, or production readback failure.
